"""
Write a Python program that prints the first 15 positive integers (starting from 1).

Print the numbers one per line using a loop and the range() function.

Hints:
Remember that by default, range() returns a sequence of integers that starts from 0 and ends at the stop parameter minus 1.

"""

for i in range(1,16):
    print(i)